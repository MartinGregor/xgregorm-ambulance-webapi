kind: Service
apiVersion: v1
metadata:
  name: xgregorm-ambulance-webapi
spec:
  selector:
    pod: xgregorm-ambulance-webapi-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: webapi-port